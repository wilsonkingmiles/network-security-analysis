# Wireshark HTTPS/TLS Summary

Wireshark was used to capture and filter HTTPS/TLS traffic. DNS results from `nslookup` were compared with packet-capture observations to validate server addresses.

An I/O graph showed a large burst of traffic when pages were initially loaded, followed by lower background or keep-alive activity. The exercise connected DNS resolution, encrypted web traffic, packet metadata, and traffic-volume interpretation.
